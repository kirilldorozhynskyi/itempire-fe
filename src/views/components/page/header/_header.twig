{#
 # File: /src/views/components/page/header/_header.twig
 # Project: itempire-fe
 # Version: 1.0.0
 # Created Date: Tuesday, March 31st 2026, 17:21:00
 # Author: Kirill Dorozhynskyi - kyrylo.dorozhynskyi@justdev.org
 # -----
 # Last Modified: Tuesday, July 21st 2026 15:48:20
 # Modified By: Kirill Dorozhynskyi
 # -----
 # Copyright (c) 2026 justDev
 #}

<page-header class="z-999 py-5 lg:py-6 bg-white">
	<template #default="{ toggleMenu, menuOpened, activeSubmenu, activeSubmenuItem, openSubmenu, openSubmenuItem, closeSubmenu }">
		<div class="container max-xl:max-w-full">
			<div class="offset flex items-center gap-10 xl:gap-16">
				<a class="" href="/" aria-label="{{ site_title }}">
					{% if logoRaw %}
						{{ source(rootDir ~ '/public/assets/images/brand/logo.svg')|raw }}
					{% else %}
						{{ svg(logo) }}
					{% endif %}
				</a>

				<div id="primary-navigation">
					{% include 'main_navigation.twig' %}
				</div>

				<div class="ms-auto flex items-center gap-2">
					<button class="btn-outline-neutral p-1.5 relative">
						<div
							class="absolute translate-x-1/2 -translate-y-1/2 bg-purple text-white rounded-full flex items-center tracking-tight leading-none font-medium text-sm justify-center min-w-6 h-6 -top-0.25 right-0.75"
						>
							9
						</div>

						{{ sprite('shopping-bag', 'text-2xl!') }}
					</button>
					<button class="btn-outline-neutral p-1.5">{{ sprite('search', 'text-2xl!') }}</button>
					<button class="btn-outline-neutral p-1.5">{{ sprite('user', 'text-2xl!') }}</button>
					<button class="btn-outline-neutral px-2 py-2.75">EN</button>
					{% include paths.btn_ex with {
						button: header.button
					} only %}
				</div>

				<div class="relative z-3 lg:hidden">
					<button
						type="button"
						class="flex h-10 w-10 cursor-pointer flex-col items-center justify-center gap-1 hover:bg-transparent"
						:aria-controls="'primary-navigation'"
						:aria-expanded="menuOpened ? 'true' : 'false'"
						:aria-label="menuOpened ? 'Close menu' : 'Open menu'"
						@click="toggleMenu()"
					>
						<span class="h-0.5 w-6 origin-center transition bg-black" :class="menuOpened ? 'translate-y-[2.5px] rotate-45' : ''"></span>
						<span class="h-0.5 w-6 origin-center transition bg-black" :class="menuOpened ? '-translate-y-[2.5px] -rotate-45' : ''"></span>
					</button>
				</div>
			</div>
		</div>
	</template>
</page-header>
